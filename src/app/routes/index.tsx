import React from 'react';
import App from '../../App';

// This is a placeholder for future routing logic (e.g., using react-router-dom)
export const AppRoutes: React.FC = () => {
  return <App />;
};
